use_frameworks!

target 'SwiftySensors iOS' do
    platform :ios, '8.4'
    
    pod 'Signals', '~> 4.0'
end

target 'SwiftySensors macOS' do
    platform :osx, '10.11'
    
    pod 'Signals', '~> 4.0'
end

target 'SwiftySensorsExample' do
    platform :ios, '8.4'
    
    pod 'Signals', '~> 4.0'
end

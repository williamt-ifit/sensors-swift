use_frameworks!

target 'SwiftySensors iOS' do
    platform :ios, '8.4'
    
    pod 'Signals', '~> 4.0'
end

target 'SwiftySensors OSX' do
    platform :osx, '10.10'
    
    pod 'Signals', '~> 4.0'
end

target 'SwiftySensorsExample' do
    platform :ios, '8.4'
    
    pod 'Signals', '~> 4.0'
end

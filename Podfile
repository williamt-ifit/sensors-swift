use_frameworks!

target 'SwiftySensors iOS' do
    platform :ios, '8.4'
    
    pod 'Signals', '~> 5.0'
end

target 'SwiftySensors macOS' do
    platform :osx, '10.13'
    
    pod 'Signals', '~> 5.0'
end

target 'SwiftySensors tvOS' do
    platform :tvos, '11.2'
    
    pod 'Signals', '~> 5.0'
end

target 'SwiftySensorsExample' do
    platform :ios, '8.4'
    
    pod 'Signals', '~> 5.0'
end
